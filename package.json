{
  "name": "raol-ui-rest-api",
  "version": "5.1.1",
  "description": "Minimalist plugin-based REST API framework. Pure JSON responses, no UI components.",
  "type": "module",
  "main": "api-server.js",
  "scripts": {
    "start": "node api-server.js",
    "dev": "node --watch api-server.js",
    "build": "npm install",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["api", "rest", "express", "nodejs", "json", "minimalist", "plugin-based"],
  "author": "RaolByte",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "chalk": "^5.3.0",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
